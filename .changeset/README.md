# Changesets

The five publishable `@angee` packages are a fixed group because they form one layered release surface: leaf changes can affect every package above them, so their versions advance together.
